Notebooks for data analysis and model experiments.

To reproduce the results, start running the code seperately with CNN, NN, LSTM.
The single parameter test with NN approach is recorded in code seperatelly, where are shown seperatelly.

The prediction code is CNN-LSTM Model.
